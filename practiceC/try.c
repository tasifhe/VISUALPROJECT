#include <stdio.h>

int main()
{
    //A example of input and output, naming a variable
    int number;
    printf("enter number");
    scanf("%d",&number);
    printf("his number is : %d", number);
    return 0;
}