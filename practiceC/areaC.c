#include<stdio.h>

//area of a cicle
int main()
{
    float radius;
    float pi = 3.1416;
    printf("enter value of radius");
    scanf("%f", &radius,pi);

    printf("area of circe : %f", pi * radius * radius);
    return 0;
}